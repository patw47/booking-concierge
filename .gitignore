# === Secrets — NEVER commit ===
.env
.env.*
!.env.example

# Google service account key + OAuth tokens
*-agent-key.json
*.serviceaccount.json
*-credentials.json
token.json
*_token.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
*.log

# OS
.DS_Store
Thumbs.db

# Orchestration metadata
.ticket-id

# Sprint summaries (archived in Obsidian)
.sprint-*-summary.md

# Local logs — transcripts contain PII (name + email of guests)
var/
