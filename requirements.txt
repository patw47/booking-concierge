# Core — version pinned to avoid SmallWebRTCTransport choppy-audio regression (fixed in 1.2.0)
pipecat-ai[webrtc,runner]==1.4.0

# Environment
python-dotenv>=1.0.0

# Google Sheets + Gmail (Sprint 2)
# google-api-python-client>=2.0
# google-auth-httplib2>=0.1.0
# google-auth-oauthlib>=0.5.0
