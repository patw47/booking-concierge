"""
Tool: get_disponibilites
Reads the Villa Eden Bleu Google Sheet calendar and returns available date ranges.
Implementation: Sprint 2.
"""


async def get_disponibilites(checkin: str, checkout: str) -> dict:
    """
    Args:
        checkin: ISO date string (YYYY-MM-DD)
        checkout: ISO date string (YYYY-MM-DD)
    Returns:
        {"available": bool, "message": str}
    """
    raise NotImplementedError("Sprint 2")
